## SafePath AI — Citizen backend API
## FastAPI service. Python 3.11+ recommended (tested on 3.14).

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# DB
sqlalchemy==2.0.36
asyncpg==0.30.0
psycopg[binary]>=3.2.10
geoalchemy2==0.16.0
alembic==1.14.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-multipart==0.0.20

# Storage (S3-compatible impl present, unused by default — see services/storage)
boto3==1.35.90

# Utilities
email-validator==2.2.0
python-dotenv==1.0.1
Pillow==11.0.0

# AI (only required when AI_PROVIDER=yolov8 — see app/services/ai/yolo_service.py).
# torch is NOT pinned to a specific build here: install a CUDA build
# yourself first if you have a GPU (e.g. `pip install torch --index-url
# https://download.pytorch.org/whl/cu124`), THEN `pip install -r
# requirements.txt` — pip won't replace an already-satisfied torch with
# the CPU-only default from PyPI. Skip this whole section (leave
# AI_PROVIDER=mock) if you don't need real inference.
ultralytics==8.4.126

# Testing
pytest==8.3.4
pytest-asyncio==0.25.1
httpx==0.28.1
