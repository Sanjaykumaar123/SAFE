import { MapScreen } from '@/components/map/MapScreen';

/** Full-screen live map tab (section 10) — see `MapScreen` for the live
 * location, hazard markers, filters, and Safe Route implementation. */
export default function Map() {
  return <MapScreen />;
}
